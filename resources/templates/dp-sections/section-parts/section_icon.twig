{% if (layout.section_icon) %}
	<div class="danzerpress-icon-img {{ layout.text_align }}" data-aos="fadeIn">
		<img alt="Image for {{ post.title }}" src="{{ layout.section_icon }}">
	</div>
{% endif %} 